import Dashboard from "./pages/Dashboard";

// App.tsx will grow into a router (react-router) once we have more than
// one page -- Phase 2 adds "Enterprise Systems", and that's when we'll
// introduce routing. For Phase 1, one page is honest and simple.
export default function App() {
  return <Dashboard />;
}
